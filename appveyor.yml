# appveyor file
# http://www.appveyor.com/docs/appveyor-yml

init:
  - git config --global core.autocrlf input

install:
  - npm install -g grunt-cli
  - npm install

build: off

test_script:
  - grunt test
